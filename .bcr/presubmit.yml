bcr_test_module:
  module_path: examples/bcr
  matrix:
    bazel:
      - 9.x
    platform:
      - ubuntu2404
      - macos_arm64
  tasks:
    verify_consumer:
      name: Verify external consumer
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//:carve"
